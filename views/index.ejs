<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Weather App</title>
</head>
<body>

    <% if(locals.err){%>
        <h1 class="text-center mt-2"> <%= err %> </h1>
    <% } else{ %>
        <h1 class="text-center mt-2">Learn the Weather in Your Location!</h1>
    <% } %>
    
    <div class="form">
        <form action="/" method="post">
            <label for="cityName">Enter City</label>
            <input type="text" id="cityName" name="cityName" placeholder="🏙️ Example: London">
            <label for="countryCode">Enter Country Code (if needed)</label>
            <input type="text" id="countryCode" name="countryCode" placeholder="🌐 Example: GB">
            <input type="submit" value="Submit">
        </form>
    </div>
    
    <% if(locals.data){%>
        
        <div class="container">
            <h2> <%= data[0].cityName %>, <%= data[0].country %> </h2>
            <div class="row">
                <div class="d-flex align-items-center">
                    <div class="col-6">
                        <img src="https://openweathermap.org/img/wn/<%= data[0].icon %>@2x.png" alt="">
                    </div>
                    <div class="col-6">
                        <p class="temp"> <%= Math.floor(data[0].temp) %>° </p>
                    </div>
                </div>
                
                <div class="col-12">
                    <p> <%= data[0].description %> </p>
                </div>
                <div class="col-6">
                    <img src="https://www.svgrepo.com/show/521793/pressure.svg" alt="" width="20px">
                    <p> <%= data[0].pressure %> hPa</p>
                </div>
                <div class="col-6">
                    <img src="https://www.svgrepo.com/show/521708/humidity.svg" alt="" width="20px">
                    <p> <%= data[0].humidity %>%</p>
                </div>
                <div class="col-6">
                    <img src="https://www.svgrepo.com/show/447532/visibility.svg" alt="" width="20px">
                    <p> <%= Math.floor((data[0].visibility)/1000) %> km</p>
                </div>
                <div class="col-6">
                    <img src="https://www.svgrepo.com/show/532896/wind.svg" alt="" width="20px">
                    <p> <%= data[0].wind %> m/s</p>
                </div>
            </div>
        </div>
        
    <%}%>

</body>
</html>